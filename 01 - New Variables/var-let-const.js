//added a .js file so i could run in terminal w/ node

function setWidth() {
    var width = 100;
    console.log(width);
}
  setWidth();

  let age = 28;
  if(age > 12) {
    let dogYears = age * 7;
    console.log(`You are ${dogYears} dog years old!`);
  }

  let height = 200;
  const key = 'abc123';