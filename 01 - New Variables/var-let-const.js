function setWidth() {
    var width = 100;
    console.log(width);
  }
  setWidth();
  

  let age = 28;
  if(age > 12) {
    var dogYears = age * 7;
    console.log(`You are ${dogYears} dog years old!`);
    if(age > 29) {
        console.log('Congrats! You\'re older than Bluey');
    }
  }

  let height = 200;
  const key = 'abc123';